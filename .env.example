# Las credenciales de Betfair NO se almacenan en variables de entorno
# Se envían como headers en cada petición HTTP:
# X-Betfair-Username: tu_usuario
# X-Betfair-Password: tu_contraseña
# X-Betfair-App-Key: tu_app_key
# X-Betfair-Session-Token: tu_session_token (alternativa a usuario/password)

# Puerto del servidor local
PORT=3001

# Dominio de Betfair (com o es)
# Usa 'es' si tu cuenta está registrada en betfair.es (España)
# Usa 'com' para cuentas internacionales (por defecto)
BETFAIR_DOMAIN=es

# SOLO PARA DESARROLLO LOCAL - Solución temporal para error de certificados SSL
# IMPORTANTE: NUNCA uses esto en producción, solo para desarrollo local
# Descomenta la siguiente línea si tienes problemas con certificados SSL:
# NODE_TLS_REJECT_UNAUTHORIZED=0
